version: '3'

tasks:
  default:
    cmds:
      - task: update

  update:
    cmds:
      - go run ./cmd/indexer/ add -name gena_grpc_server -src ../gena_grpc_server/
      - go run ./cmd/indexer/ add -name gena_rest_server -src ../gena_rest_server/
      - go run ./cmd/indexer/ add -name gena_kafka_producer -src ../gena_kafka_producer/

  run:
    cmds:
      - go run ./cmd/gena/

  build:
    cmds:
      - go build -o bin/indexer ./cmd/indexer
      - go build -o bin/gena ./cmd/gena

  check:
    deps:
      - update
    cmds:
      - rm -rf /tmp/some/check
      - go run ./cmd/gena/ -name check -opt gena_grpc_server -opt gena_rest_server -opt gena_kafka_producer -output /tmp/some/

  lint:
    desc: Run linter
    cmds:
      - go fmt ./...
      - go vet ./...

